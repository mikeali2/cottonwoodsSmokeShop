# Optional: age-gate logs (IP + user agent) when visitors click "I am 21 or older"
# Option A — Supabase: run `supabase/age_gate_logs.sql`, then set:
# VITE_SUPABASE_URL=https://xxxx.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key

# Option B — any HTTPS endpoint that accepts POST application/json:
# VITE_AGE_VERIFICATION_LOG_URL=https://your-hook.example/age-log
